import { ContenidoDirective } from './contenido.directive';

describe('ContenidoDirective', () => {
  it('should create an instance', () => {
    const directive = new ContenidoDirective();
    expect(directive).toBeTruthy();
  });
});
